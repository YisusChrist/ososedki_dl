name: Release

on:
  push:
    branches:
      - main
      - master
    paths:
      - pyproject.toml

jobs:
  release:
    uses: YisusChrist/github-actions-workflows/.github/workflows/release.yml@main
    with:
      python-version: "3.12"
    secrets: inherit
